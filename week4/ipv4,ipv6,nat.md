# 🌐 مقایسه IPv4، IPv6 و NAT + وضعیت ایران

این فایل برای آشنایی سریع و خلاصه با **IPv4**، **IPv6** و **NAT** تهیه شده است.  
هدف، درک تفاوت‌ها، مزایا، معایب، و وضعیت فعلی استفاده از IPv6 در ایران است.

---

## 📘 1. آدرس IP چیست؟
آدرس IP (Internet Protocol Address) شناسهٔ عددی هر دستگاه متصل به اینترنت است؛ شبیه آدرس پستی برای رسیدن بسته‌ها.

مثال‌ها:
- IPv4: `192.168.1.1`
- IPv6: `2001:0db8:85a3:0000:0000:8a2e:0370:7334`

---

## 🔢 1.1 تعریف IPv4 و IPv6

<div dir="rtl" align="right">

<h3>🧩 IPv4 چیست؟</h3>
<p>
IPv4 یا <b>Internet Protocol version 4</b> نسل چهارم پروتکل IP است (از ۱۹۸۱).<br/>
از <b>آدرس‌های ۳۲ بیتی</b> استفاده می‌کند و حدود <b>۴.۳ میلیارد</b> آدرس منحصربه‌فرد می‌دهد.<br/>
نمونه آدرس: <code dir="ltr">192.168.0.10</code>&lrm;<br/>
به‌دلیل رشد کاربران، فضای IPv4 رو به اتمام رفت؛ به همین خاطر از <b>NAT</b> برای اشتراک‌گذاری یک IP عمومی بین چند دستگاه استفاده شد.
</p>

<h3>🚀 IPv6 چیست؟</h3>
<p>
IPv6 یا <b>Internet Protocol version 6</b> برای رفع محدودیت IPv4 از ۱۹۹۸ معرفی شد.<br/>
از <b>آدرس‌های ۱۲۸ بیتی</b> استفاده می‌کند (ظرفیت بسیار عظیم ~۳۴۰ سپتیلیون).<br/>
نمونه آدرس: <code dir="ltr">2001:0db8:85a3::8a2e:0370:7334</code>&lrm;<br/>
افزون‌بر فضای خیلی بزرگ‌تر، IPv6 امنیت بومی (IPsec)، پیکربندی خودکار، و سازگاری بهتر با <b>IoT</b> و <b>5G</b> دارد.
</p>

</div>

---

## 📊 2. جدول مقایسه IPv4 و IPv6

| ویژگی‌ها | IPv4 | IPv6 |
|---|---|---|
| طول آدرس | 32 بیت | 128 بیت |
| شکل نوشتاری | چهار بخش با نقطه (`192.168.1.1`) | هشت بخش با دونقطه (`2001:db8::7334`) |
| تعداد آدرس | ~۴.۳ میلیارد | بسیار عظیم (۳۴۰×۱۰²¹) |
| پیکربندی | دستی/DHCP | خودکار (SLAAC) و DHCPv6 |
| امنیت | IPsec اختیاری | IPsec به‌صورت بومی |
| نیاز به NAT | بله (معمولاً) | معمولاً نیاز نیست |
| مناسب موبایل/IoT | محدودتر | بسیار بهتر |
| مسیریابی | خوب | بهینه‌تر/ساده‌تر (بدون NAT) |

---

### 📈 2.1 مزایا و معایب IPv4
| مزایا | معایب |
|---|---|
| سازگاری کامل با تجهیزات و نرم‌افزارهای قدیمی | کمبود آدرس (وابستگی به NAT) |
| ساده و شناخته‌شده | امنیت ذاتی کمتر (IPsec اختیاری) |
| سربار هدر کوچک | محدودیت برای موبایل/IoT و end-to-end |

---

### 🚀 2.2 مزایا و معایب IPv6
| مزایا | معایب |
|---|---|
| فضای آدرس بسیار بزرگ (بدون نیاز به NAT) | برخی تجهیزات/سرویس‌ها هنوز پشتیبانی کامل ندارند |
| IPsec بومی، end-to-end واقعی | نیاز به آموزش/مهارت جدید در استقرار و عیب‌یابی |
| پیکربندی خودکار، مناسب IoT/5G | ممکن است در بعضی محیط‌ها دیباگ پیچیده‌تر باشد |

---

## ⚙️ 3. NAT چیست؟
**NAT (Network Address Translation)** ترجمهٔ آدرس‌های خصوصی داخل شبکه (مثلاً `192.168.1.x`) به یک آدرس عمومی اینترنت است.

### 🏠 مثال خیلی ساده
مودم خانه یک IP عمومی دارد، اما چند موبایل/لپ‌تاپ وصل‌اند. NAT کاری می‌کند همه با همان IP عمومی به اینترنت بروند، درحالی‌که داخل شبکه هرکدام آدرس خصوصی خودشان را دارند.

### ✅ مزایا و ❌ معایب NAT
| مزایا | معایب |
|---|---|
| صرفه‌جویی در IP عمومی | تاخیر و پیچیدگی ترجمه |
| پنهان‌سازی نسبی میزبان‌های داخلی | سختی برای بعضی سرویس‌ها (VoIP/بازی/ورود از بیرون) |
| کنتر‌ل‌پذیری ترافیک خروجی | ناسازگاری جزئی با برخی پروتکل‌ها |

---

## 🌍 4. چرا IPv6 ساخته شد؟
کمبود آدرس‌های IPv4، نیاز به end-to-end واقعی، امنیت بومی‌تر، پیکربندی خودکار، و مقیاس‌پذیری برای موبایل/IoT/5G.

---

## 🇮🇷 5. وضعیت ایران در استفاده از IPv6 (خلاصه)
- **کاربران IPv6-قابل** (۳۰ روز اخیر تا اوایل نوامبر ۲۰۲۵): حدود **۱۴–۱۵٪**؛ **ترجیح IPv6** حدود **۱۳٪**.  
- **کاربران خانگی**: عمدتاً هنوز IPv4؛ اما برخی اپراتورها/دیتاسنترها مسیر آماده‌سازی را پیش می‌برند.  
- **روند**: آهسته ولی رو به افزایش؛ ممکن است دچار نوسان‌های دوره‌ای شود.

> نکتهٔ عملی: اگر ISP/دیتاسنتر شما IPv6 ارائه می‌دهد، با فعال‌کردن IPv6 در روتر/سیستم‌عامل و تنظیم فایروال، معمولاً بدون تغییرات پیچیده از مزایای آن بهره‌مند می‌شوید.

---

## 💡 6. جمع‌بندی خیلی کوتاه
| موضوع | IPv4 | IPv6 | NAT |
|---|---|---|---|
| فضا | محدود | بسیار بزرگ | — |
| امنیت بومی | کمتر | بیشتر (IPsec) | نسبی (پشت NAT) |
| end-to-end | شکسته (به‌خاطر NAT) | برقرار | — |
| آینده | نگه‌داری/انتقال | مقصد نهایی | راهکار موقت کمبود IPv4 |

---

### ✏️ منابع پیشنهادی
- IETF: مروری بر IPv6  
- Google IPv6 Statistics  
- APNIC IPv6 Measurements (ایران)  
- RIPE NCC: بحث و ارائه‌های IPv6 در ایران
